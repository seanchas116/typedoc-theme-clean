<div>{{#compact}}
    <h3 class="member-title {{cssClasses}}">
        {{{wbr name}}}<span>{{#if isOptional}}?{{/if}}:</span>&nbsp;{{#with type}}{{>type}}{{/with}}
        {{#if defaultValue}}
            <span class="default-value">
            &nbsp;=&nbsp;
            {{defaultValue}}
            </span>
        {{/if}}
    </h3>
{{/compact}}</div>

{{> comment}}
